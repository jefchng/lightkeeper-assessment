# lightkeeper-assessment

## Install Requirements

`pip install -r requirements`

## Run

`python main.py --help`
`python main.py --filename <path-to-csv>`

## Test & Format

`isort .`
`black .`
`mypy .`
`flake8 .`
`python -m pytest --cov=company_a tests`